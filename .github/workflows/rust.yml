name: Rust

on:
  push:
    branches: [ "main" ]
    paths-ignore: ["**/*.md", "**/*.rst", "docs/**", "LICENSE*"]
  pull_request:
    branches: [ "main" ]
    paths-ignore: ["**/*.md", "**/*.rst", "docs/**", "LICENSE*"]
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  CARGO_BUILD_JOBS: 2

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - name: Cargo Fmt
        run: cargo fmt --all -- --check

  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        rust: ['stable', '1.85']

    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
    - name: Install toolchain
      uses: dtolnay/rust-toolchain@6bed0761d98439e5a578e2877258200ad565ba87
      with:
        toolchain: ${{ matrix.rust }}
    - name: Cache Cargo sources and tools
      uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2
      with:
        prefix-key: nusb-v1-sources
        cache-targets: false
    - name: Run tests
      run: |
        cargo test --verbose
        cargo test --verbose --features tokio
        cargo test --verbose --features smol
        cargo test --verbose --features smol,tokio

  check:
    strategy:
      matrix:
        target: [aarch64-linux-android, armv7-linux-androideabi, wasm32-unknown-unknown]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
    - name: Install toolchain
      uses: dtolnay/rust-toolchain@6bed0761d98439e5a578e2877258200ad565ba87
      with:
        targets: ${{ matrix.target }}
    - name: Cache Cargo sources and tools
      uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2
      with:
        prefix-key: nusb-v1-sources
        cache-targets: false
    - name: check
      run: |
        cargo check --target ${{ matrix.target }}
        cargo check --target ${{ matrix.target }} --features tokio
        cargo check --target ${{ matrix.target }} --features smol
        cargo check --target ${{ matrix.target }} --all-features

  rustdoc:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
    - name: Install toolchain
      uses: dtolnay/rust-toolchain@6bed0761d98439e5a578e2877258200ad565ba87
      with:
        toolchain: nightly
    - name: Cache Cargo sources and tools
      uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2
      with:
        prefix-key: nusb-v1-sources
        cache-targets: false
    - name: rustdoc
      run: RUSTDOCFLAGS=--cfg=docsrs cargo +nightly doc --all-features --no-deps
